
{{#if user.userID}}
	<div class="card card-body" style="width:73vw"> 
		<h2>Welcome, {{user.name}}.</h4>
	</div>
{{/if}}
<body style="background:#333;margin:0;padding:0">

	<div class="card card-body" style="width:73vw">	
		
		{{#each announcements}}

		<a class="card card-body" style="padding:0; margin-bottom:20px; width:70vw; overflow:hidden" {{#if ../user.admin}} href="announcement/{{id}}"{{/if}}>
			<h4 style="margin-left:15px; margin-top:15px">{{Title}}</h4>
			<h5 style='white-space: pre-line;'>{{{Body}}}</h5>
			<table>
			<tr>
				<td><p> {{timeago}}</p><td>
				<td><p style="text-align:right"> {{time}} </p><td>
			</tr>
			</table>
		</a>
		{{/each}}
		</p>
	</div>
</body>